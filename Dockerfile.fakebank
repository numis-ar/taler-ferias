FROM python:3.11-slim

WORKDIR /app

# Install dependencies
RUN pip install flask requests

# Copy the bank wrapper script
COPY fake-bank.py /app/fake-bank.py

# Make it executable
RUN chmod +x /app/fake-bank.py

EXPOSE 8082

CMD ["python3", "/app/fake-bank.py"]
